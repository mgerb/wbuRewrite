
import * as types from '../constants';

const defaultState = {
    email: '',
    firstName: '',
    lastName: '',
};

function user(state = defaultState, action) {
    switch (action.type) {
    }

    return state;
}

export default user;
